version: 2
jobs:
  build:
    docker:
      - image: circleci/openjdk:11-jdk-node-browsers
    steps:
      - checkout
      - run: ./gradlew assemble
      - run: ./gradlew build
workflows:
  version: 2
  build_and_test:
    jobs:
      - build
